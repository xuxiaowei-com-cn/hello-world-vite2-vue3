import Ref from '../../views/demo/Ref.vue'
import Unref from '../../views/demo/Unref.vue'

const demoRef = {
    path: '/demo/ref',
    name: 'Ref',
    component: Ref,
}

const demoUnref = {
    path: '/demo/unref',
    name: 'Unref',
    component: Unref,
}

export {
    demoRef,
    demoUnref,
}
